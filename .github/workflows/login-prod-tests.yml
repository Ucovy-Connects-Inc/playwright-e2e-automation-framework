
name: Github Actions For Testing 

on:
  workflow_dispatch:
    inputs:
      test_tags:
        description: 'Test tags to run (e.g., @positiveLogin, @negativeLogin, @smoke)'
        required: false
        default: '@positiveLogin'
        type: string
      environment:
        description: 'Environment to test (qa, staging, prod)'
        required: false
        default: 'qa'
        type: choice
        options:
        - qa
        - staging
        - prod
  schedule:
    - cron: '0 */3 * * *'  # Runs every 3 hours
  push:
    branches:
      - main
      - feature/**

jobs:
  login-prod-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 120  # Increased timeout for PROD environment

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install Dependencies
        run: |
          if [ -f package-lock.json ]; then
            echo "📦 Using npm ci (package-lock.json found)"
            npm ci
          else
            echo "📦 Using npm install (no package-lock.json)"
            npm install
          fi

      - name: 🎭 Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: ⚙️ Setup Environment Variables
        run: |
          # For scheduled runs (every 3 hours), always use QA
          if [ "${{ github.event_name }}" == "schedule" ]; then
            ENV_INPUT="qa"
            echo "🕒 Scheduled run - Using QA environment"
          else
            ENV_INPUT="${{ github.event.inputs.environment || 'qa' }}"
            echo "👤 Manual/Push run - Using selected environment: ${ENV_INPUT}"
          fi
          
          echo "ENV=${ENV_INPUT}" >> $GITHUB_ENV
          echo "CI=true" >> $GITHUB_ENV
          echo "HEADLESS=true" >> $GITHUB_ENV
          echo "PLAYWRIGHT_HEADLESS=true" >> $GITHUB_ENV
          
          # Set environment URLs with proper configuration
          case "${ENV_INPUT}" in
            "qa")
              echo "QA_BASE_URL=https://ecommerce-playground.lambdatest.io" >> $GITHUB_ENV
              echo "ACTION_TIMEOUT=30000" >> $GITHUB_ENV
              echo "TEST_TIMEOUT=60000" >> $GITHUB_ENV
              ;;
            "prod")
              echo "PROD_BASE_URL=https://my.marathon-health.com" >> $GITHUB_ENV
              echo "ACTION_TIMEOUT=60000" >> $GITHUB_ENV  # Increased for PROD
              echo "TEST_TIMEOUT=120000" >> $GITHUB_ENV   # Increased for PROD
              ;;
            "staging")
              echo "STAGING_BASE_URL=https://stage.my.marathon-health.com" >> $GITHUB_ENV
              echo "ACTION_TIMEOUT=45000" >> $GITHUB_ENV
              echo "TEST_TIMEOUT=90000" >> $GITHUB_ENV
              ;;
          esac

      - name: 🌐 Test URL Connectivity
        run: |
          echo "🔍 Testing URL connectivity before running tests..."
          case "${{ env.ENV }}" in
            "qa")
              URL="${{ env.QA_BASE_URL || 'https://ecommerce-playground.lambdatest.io' }}"
              ;;
            "prod")
              URL="${{ env.PROD_BASE_URL || 'https://my.marathon-health.com' }}"
              ;;
            "staging")
              URL="${{ env.STAGING_BASE_URL || 'https://stage.my.marathon-health.com' }}"
              ;;
          esac
          
          echo "Testing URL: $URL"
          
          # Test connectivity with retries
          for i in {1..3}; do
            if curl -s --head --fail --connect-timeout 30 --max-time 60 "$URL" > /dev/null; then
              echo "✅ URL is reachable (attempt $i)"
              break
            else
              echo "❌ URL unreachable (attempt $i/3)"
              if [ $i -eq 3 ]; then
                echo "::warning::URL connectivity test failed after 3 attempts"
              else
                sleep 10
              fi
            fi
          done

      - name: 🔧 Debug Environment Setup
        run: |
          echo "Trigger: ${{ github.event_name }}"
          echo "Environment: $ENV"
          echo "QA Base URL: $QA_BASE_URL"
          echo "Staging Base URL: $STAGING_BASE_URL" 
          echo "Prod Base URL: $PROD_BASE_URL"
          echo "Action Timeout: $ACTION_TIMEOUT"
          echo "Test Timeout: $TEST_TIMEOUT"
          echo "Node Version: $(node --version)"
          echo "NPM Version: $(npm --version)"
          echo "Playwright Version: $(npx playwright --version)"
          echo "Working Directory: $(pwd)"

      - name: 🧪 Run Tests (All for Scheduled, Selected for Manual)
        run: |
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "🕒 Scheduled Run: Running ALL tests (Login + Homepage + AI Visual)"
            ENV=${{ env.ENV }} npx playwright test \
              --project=chromium-ai-visual \
              --reporter=html,line \
              --timeout=${{ env.TEST_TIMEOUT || '90000' }} \
              --retries=0 \
              --workers=1
          else
            TEST_TAGS="${{ github.event.inputs.test_tags || '@positiveLogin' }}"
            echo "👤 Manual/Push Run: Running tests with tags: ${TEST_TAGS}"
            ENV=${{ env.ENV }} npx playwright test \
              --project=chromium-ai-visual \
              --grep="${TEST_TAGS}" \
              --reporter=html,line \
              --timeout=${{ env.TEST_TIMEOUT || '90000' }} \
              --retries=0 \
              --workers=1
          fi
        env:
          ENV: ${{ env.ENV }}
          QA_BASE_URL: ${{ env.QA_BASE_URL }}
          STAGING_BASE_URL: ${{ env.STAGING_BASE_URL }}
          PROD_BASE_URL: ${{ env.PROD_BASE_URL }}
          ACTION_TIMEOUT: ${{ env.ACTION_TIMEOUT }}
          TEST_TIMEOUT: ${{ env.TEST_TIMEOUT }}
          PLAYWRIGHT_HEADLESS: true
          CI: true

      - name: 📊 Upload Test Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-report-${{ env.ENV }}-${{ github.run_number }}
          path: |
            playwright-report/
            test-results/
          retention-days: 14

      - name: 📋 Upload Screenshots on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-screenshots-${{ env.ENV }}-${{ github.run_number }}
          path: test-results/
          retention-days: 7

      - name: 📤 Upload Test Traces on Failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: test-traces-${{ env.ENV }}-${{ github.run_number }}
          path: test-results/**/*.zip
          retention-days: 7

      - name: 📧 Notify on Failure
        if: failure()
        run: |
          echo "::error::Tests failed in ${{ env.ENV }} environment"
          echo "::error::Trigger: ${{ github.event_name }}"
          echo "::error::If this is PROD environment, the site might need warm-up time"
          echo "::error::Check the uploaded artifacts for details"

      - name: ✅ Test Summary
        if: always()
        run: |
          echo "## Test Results - ${{ env.ENV }} Environment" >> $GITHUB_STEP_SUMMARY
          echo "- **Trigger**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment**: ${{ env.ENV }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ github.event_name }}" == "schedule" ]; then
            echo "- **Test Scope**: All Tests (Login + Homepage + AI Visual)" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Test Tags**: ${{ github.event.inputs.test_tags || '@positiveLogin' }}" >> $GITHUB_STEP_SUMMARY
          fi
          echo "- **Timeout**: ${{ env.TEST_TIMEOUT || '90' }} seconds per test" >> $GITHUB_STEP_SUMMARY
          echo "- **Retries**: 1 (for stability)" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- **Status**: ✅ All Tests Passed" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Status**: ❌ Some Tests Failed" >> $GITHUB_STEP_SUMMARY
            echo "- **Note**: PROD environment may need warm-up, retry if needed" >> $GITHUB_STEP_SUMMARY
          fi
